import { DepartmentManager } from "./classes/DepartmentManager";

const manager = new DepartmentManager();

/**
 * Import .csv
 * Update current .csv row (in a loop)
 *  - Pass that row into departmentmanager
 */
